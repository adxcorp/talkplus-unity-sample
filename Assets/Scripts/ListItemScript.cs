using UnityEngine;
using UnityEngine.UI;

public class ListItemScript : MonoBehaviour
{
    public RectTransform itemRect, messageRect;
    public Text userText, messageText, dateText, unreadCountText;
    public Button actionButton;
}